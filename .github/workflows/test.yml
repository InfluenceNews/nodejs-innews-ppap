name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"
      - run: pip install -r requirements.txt
      - run: python3 main.py
